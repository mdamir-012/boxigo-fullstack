import React, { useState } from "react";

const CategoryDetails = ({ inventoryItemDetails }) => {
  console.log(inventoryItemDetails.name);

  

  return <div>{/* <p>name: {name}</p> */}</div>;
};

export default CategoryDetails;
